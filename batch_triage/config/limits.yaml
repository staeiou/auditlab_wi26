openai_batch:
  max_requests_per_file: 50000
  max_file_size_mb: 200
  allowed_completion_windows: ["24h"]
  require_single_model_per_file: true

wandb:
  # Guardrails (conservative). Keep logging lightweight.
  min_seconds_between_metric_logs: 60
  min_seconds_between_artifact_uploads: 60

