
project/
├─ config/
│  ├─ config.yaml                 # main config: models, batch endpoint, polling, W&B project, paths
│  ├─ pricing.yaml                # model pricing (input/output $/1M) used for cost estimation
│  ├─ limits.yaml                 # OpenAI Batch limits + W&B upload cadence limits
│  └─ logging.yaml                # what fields to store, artifact names, upload intervals
│
├─ batch/                         # generated by generate_batch_payloads.py (per model)
│  ├─ gpt-5.1/
│  │  ├─ gpt-5.1_requests.jsonl
│  │  ├─ gpt-5.1_requests.csv
│  │  └─ ...
│  ├─ gpt-4o/
│  └─ gpt-4o-mini/
│
├─ scripts/
│  ├─ generate_batch_payloads.py  # reads config/config.yaml; writes batch/<model>/...
│  ├─ run_openai_batch.py         # “main” CLI entrypoint (small): orchestrates submission + polling
│  └─ replay_vllm.py              # (optional) JSONL replay runner for vLLM server
│
├─ src/
│  ├─ __init__.py
│  ├─ config_loader.py            # load/validate YAMLs; merge overrides; default paths
│  ├─ cost_estimator.py           # token estimation + pricing.yaml -> cost estimate
│  ├─ openai_batch/
│  │  ├─ __init__.py
│  │  ├─ client.py                # wrappers: upload_file, create_batch, get_batch, download_file
│  │  ├─ validator.py             # enforce limits from limits.yaml (50k req, 200MB, one-model-per-file)
│  │  └─ parser.py                # parse OpenAI batch output jsonl -> compact results rows
│  │
│  ├─ state/
│  │  ├─ __init__.py
│  │  └─ state_store.py           # read/write state.json (resume), per-model state machine helpers
│  │
│  ├─ wandb_logging/
│  │  ├─ __init__.py
│  │  ├─ run_manager.py           # wandb.init config, tags, run metadata
│  │  ├─ artifacts.py             # upload inputs/outputs/state files as artifacts on a schedule
│  │  └─ tables.py                # optional: wandb.Table logging for small runs
│  │
│  └─ io_utils/
│     ├─ jsonl.py                 # streaming jsonl reader/writer
│     └─ paths.py                 # consistent naming: safe_model_dirname, output paths
│
├─ state/
│  ├─ openai_batch_state.json     # runtime state (created by run_openai_batch.py)
│  └─ checkpoints/                # optional: per-model partial outputs, timestamps
│
├─ outputs/
│  ├─ openai/
│  │  ├─ gpt-5.1/
│  │  │  ├─ output.jsonl
│  │  │  ├─ errors.jsonl
│  │  │  └─ results_compact.csv
│  │  └─ ...
│  └─ vllm/
│     └─ ...
│
├─ pyproject.toml (or requirements.txt)
└─ README.md

